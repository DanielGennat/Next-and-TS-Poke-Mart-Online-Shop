export {}
/* interface IAllItems {
    name: string;
    url: string;
  } []; */

/* export default function ItemList(allItems:{name:"", url:""}) {
    return(
        {allItems.map((item:{name:"", url:""}) => {
            return ( <div>item.name</div>);
            })
        }
    );
} */

/* export default function ItemList(pokeapiURL:any) {
    return <p>{pokeapiURL}</p>
} */